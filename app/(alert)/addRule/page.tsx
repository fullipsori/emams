import { Metadata } from "next";
import VpnState from "@/app/components/vpnState/vpnState";

export const metadata: Metadata = {
  title: "Add Alert Rule",
};

export default function AddAlertRule() {
  return (
    <div>
      <h1>Add Alert Rule</h1>
    </div>
  );
}
